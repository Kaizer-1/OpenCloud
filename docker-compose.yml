services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - opencloud-data:/data
    environment:
      - SPRING_DATASOURCE_URL=jdbc:sqlite:/data/opencloud.db
    restart: unless-stopped

volumes:
  opencloud-data:
